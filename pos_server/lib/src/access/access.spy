class: Access
table: access
fields:
  name: String
  orderCreation: bool
  orderCreationNotif: bool, default=true
  orderPayment: bool
  orderItemsPayment: bool
  buildingId: int?, relation(parent=building),required


indexes:
  company_name_idx:
    fields: name
    unique: true