class: BTable
table: b_tables
fields:
  ### The number of the building.
  number: int
  seatsMax: int,default=4
  status: TableStatus?,!persist ### This field is not persisted in the database.
  buildingId: int?, relation(parent=building),required