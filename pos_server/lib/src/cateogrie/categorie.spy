class: Categorie
table: categorie
fields:
  id: UuidValue, defaultPersist=random, defaultModel=random
  name: String,
  description: String?
  buildingId: UuidValue, relation(parent=building),


indexes:
  categorie_buildingId_idx:
    fields: buildingId